---
import {
  default as NodeMysqlGuide,
  frontmatter,
  getHeadings,
} from '../../../../docs/presets/node-mysql.mdx';
import { DocsLayout } from 'reallysimpledocs/components';

const page = {
  slug: 'presets/node-mysql',
  file: 'presets/node-mysql.mdx',
  extension: '.mdx',
  path: '/docs/presets/node-mysql',
  markdownPath: '/docs/presets/node-mysql.md',
  title: frontmatter.title,
};
const headings = (await getHeadings()).map(({ depth, slug, text }) => ({
  depth,
  id: slug,
  text,
}));
---

<DocsLayout page={page} headings={headings}>
  <NodeMysqlGuide />
</DocsLayout>
